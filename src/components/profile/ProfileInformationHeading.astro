---
import type {Profile} from "../../profile";

export interface Props extends Pick<Profile, "names" | "role"> {}

const {names, role} = Astro.props;
---

<h1 class="font-bold text-6xl lg:text-7xl">{names.join(" ")}</h1>
<p class="text-xl lg:text-4xl">{role}</p>